import { FC } from 'react';

const HeroWrapper: FC = () => {
    return (
        <div className='h-[100vh] w-full flex justify-center items-center text-center'>
            <h1 className='text-2xl'>Sketch craft</h1>
        </div>
    );
};

export default HeroWrapper;
